@model IEnumerable<Biblioteca_Tomar.Models.Requisicoes>

@{
    ViewData["Title"] = "Index";
}

<h1>Requisições existentes</h1>

<p>
    <a asp-action="Create">Adicionar uma nova requisição</a>
</p>
<div class="tableFixHead">
    <table class="table tableText">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Requisitante)
                </th>
                <th>
                    Funcionário Requisição
                </th>
                <th>
                    Funcionário Devolução
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Data)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Multa)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Requisitante.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FuncionarioInicioRequisicao.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FuncionarioFimRequisicao.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Data)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Multa)
                    </td>
                    <td>
                        <a asp-action="Revoke" asp-route-id="@item.Id">Devolução</a> |
                        <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>

